/**
 * @fileoverview Testing domain barrel — all test-specific exports.
 *
 * This domain is fully self-contained and can be extracted into
 * a standalone application if needed.
 *
 * Structure:
 *   testing/
 *     pages/   — UITests page
 *     suites/  — Test suite definitions
 *     routes.tsx — Test route definitions
 */
export { testRoutes } from './routes';
