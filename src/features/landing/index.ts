/**
 * @fileoverview Landing domain barrel — all landing-specific exports.
 *
 * This domain is fully self-contained and can be extracted into
 * a standalone application if needed.
 *
 * Structure:
 *   landing/
 *     components/  — PricingSection, SplashScreen
 *     pages/       — Landing, About, Contact, Privacy, Terms, Security
 *     routes.tsx   — Landing route definitions
 */
export { landingRoutes } from './routes';
